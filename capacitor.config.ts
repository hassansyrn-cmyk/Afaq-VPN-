import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.afaq.vpn', appName: 'Afaq VPN', webDir: 'dist', android: { allowMixedContent: false } };
export default config;
